# إدارة البيانات والتحليل
pandas>=1.5.0
numpy>=1.23.0

# واجهة المستخدم التفاعلية
dash>=2.10.0
dash-bootstrap-components>=1.3.0
plotly>=5.13.0

# النماذج التنبؤية والتحليل الإحصائي
prophet>=1.1.2       # لتوقعات Prophet
statsmodels>=0.13.5  # لنماذج ARIMA والتحليل الإحصائي

# تقارير PDF و Excel و HTML
reportlab>=3.6.12
openpyxl>=3.1.2
xlrd>=2.0.1
Jinja2>=3.1.2         # لقوالب التقارير HTML

# دعم رفع الملفات ومعالجة البيانات
python-multipart>=0.0.5

# تسجيل السجلات واللوجنج
loguru>=0.7.0

# أدوات مساعدة أخرى
pyyaml>=6.0            # التعامل مع ملفات YAML للإعدادات
requests>=2.28.2       # لجلب البيانات من API (مصادر خارجية)
urllib3>=1.26.15

# اختبارات (اختياري)
pytest>=7.3.1
pytest-cov>=4.0.0

# متطلبات خاصة (إن وجدت)
gunicorn>=20.1.0       # تشغيل السيرفر في بيئة الإنتاج (اختياري)
